# CI Technologies website

Static single-page marketing site (`index.html`) deployed via GitHub Pages
(`CNAME` → citechnologies.io).

## Agent skills

### Issue tracker

Issues live as GitHub issues in `PilotGuy13/PilotGuy13-citech-website`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-role vocabulary, label strings unchanged. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
